import 'package:flutter/material.dart';

class ChatPage extends StatelessWidget {
  static final pageName = '/chat';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Chat Page'),
      ),
    );
  }
}
